# 📱 Öğrenci — Bireysel Çalışma Ekranları (M08 `study` feature)

> **Feature:** `mobile/lib/features/study/` (`data` / `domain` / `presentation`) · **Backend:** `/api/study` (bkz. [`../modules/m08_study.md`](../modules/m08_study.md))
> **Rol:** Öğrenci (`Student`). Rol bazlı `redirect` öğrenciyi `/student-home`'a yönlendirir. Öğrenci StudentId'si M03 `by-user` ile çözülür; profil yoksa `SelfRegistered` olarak oluşturulur.
> **Veri:** 🟢 gerçek API. **Güncelleme:** 2026-08-19

---

## Ekranlar

| Route | Sayfa (dosya) | State | İçerik |
|-------|---------------|-------|--------|
| `/student-home` | `student_home_page.dart` (`StudyHomeCubit`) | dashboard | **Premium yeniden tasarım (2026-07-19), sayaç üste taşındı (Task 2, 2026-07-21).** Bilgi hiyerarşisine göre (bkz. [`../roles/ogrenci_ux.md`](../roles/ogrenci_ux.md) §3/§5): (0) **büyük sayaç/başlat kartı en üstte** (0 tık ilkesi — "Çalışmaya Başla", navy→mavi gradient tam genişlik CTA, `/study/timer`'a doğrudan açar), (1) selamlama + **pozitif/güne özel motivasyon alt başlığı** ("Bugün 45dk daha çalışırsan hedefini tamamlayacaksın"), (2+3) tek **premium hero bloğu** (navy→mavi degrade, radius 26, ışıltılı gölge): solda **dairesel ilerleme halkası** (`CustomPainter` — bugünkü hedef %'i; ortada bugünkü süre + "bugün"), sağda başlık + "%yüzde · Hedef X" ipucu + buzlu cam **seri rozeti** (🔥 + "$n gün seri · bugün de tamam/bugün çalış, bozma") — eski yan yana iki kartın yerini alır, (3.5) **Bugünün planı** (Ç-06 occurrence'ları; başlıkta `tamam/toplam` rozeti; her satır: saat çipi + ders·konu + dolu primary play düğmesi, tamamlanan ✓ + soluk; dokununca sayaç derse bağlı başlar), (4) **ikincil kısayollar: Hedefler · Rozetler · Manuel Ekle** (Deneme Gir/Geçmiş artık Performans sekmesine ait — Task 3) 2 sütunlu `Wrap` yatay kart ızgarası (3 öğe, tonlu gradient ikon kutuları), (5) yaklaşan ders (varsa) + İlerlemen (bu hafta + rekor seri — yatay ikon+metin kartları), (6) son deneme + son çalışmalar. **Birleşik premium kart dili:** tüm kartlar cam gradyanlı beyaz yüzey + iki katmanlı gölge (yakın key + geniş ambient); ikonlar `_IconChip` **dolu gradient madalyon** (renk→koyu degrade + renkli ışıma gölgesi); bölüm başlıklarında ince gradient vurgu çubuğu; hero + CTA'da dekoratif ışıma daireleri. **Mikro-etkileşim:** CTA/plan satırı/işlem kutuları basılıyken `scale 0.97` (`_Pressable`, `animations.md` §4). Pull-to-refresh. |
| `/student/performance` | `student_tests_page.dart` | özet | **Performans sekmesi** (eski `/student/tests` redirect'lenir; Task 3'te genişletildi — Çalışmalarım'ın haftalık/ders-konu analizini absorbe etti, `/student/studies` retire edildi): istatistik (deneme/ort. net/en iyi net) + "Deneme gir" (→test) + net trend çubuk grafiği + ders bazlı analiz (trend oku ↑/↓) + son denemeler + **Haftalık analiz** (günlük çubuk grafik, `getWeeklySummary`) + **Ders → konu kırılımı** (tüm zamanlar, açılır ders→konu: her ders bir satır + toplam süre; dokununca `AnimatedSize`/`AnimatedRotation` ile o dersin konuları + konuya göre süre çubukları açılır — tamamlanmış `listSessions`'tan istemci tarafında hesaplanır) + **Analiz & Gelişim** girişleri (Detaylı analiz → `/study/history`, Gelişimim → `/student/progress`). Analiz `listTests`+`getWeeklySummary`+`listSessions`'tan türetilir. Liste satırları (son denemeler + ders analizi) ortak `StudySessionTile` deseniyle hizalı: soldaki tonlu 40px ikon kutusu, analizde satır ayraçları. |
| `/student/lessons` | `student_calendar_page.dart` | takvim | **Derslerim sekmesi** (eski `/student/calendar` redirect'lenir; 2026-07-08; başlık "Derslerim" + "Ders araçları" bölümü eklendi — Task 4, 2026-07-21): öğrencinin **birleşik ders programı**. `SchedulingRepository.getStudentCalendar` (öğretmen dersleri + kendi `StudyScheduleEntry` girdileri, tekrarlar backend'de genişletilmiş). **Öğretmen takvim ekranıyla birebir aynı takvim:** `SfCalendar` **Aylık/Haftalık/Günlük** geçişi + `‹ tarih Bugün ›` gezinme çubuğu → altında **öğretmen paneli stili** seçili gün listesi (renk çubuğu + "Öğretmen/Kendi" pill + saat + tekrar/konum). Öğretmen dersleri salt-okunur + kilit (öncelikli); kendi dersleri ⋮ menü (Düzenle/Sil). "Ders ekle" FAB → `StudyEntryFormSheet` (**tam ekran**, öğretmen `LessonFormSheet` düzeniyle uyumlu: tek/tekrarlı günlük·haftalık·aylık + ders adı/saat/**hatırlatma**/not). Öğretmen dersinin saatine çakışan kendi dersi eklenemez (`scheduling.teacher_conflict` → uyarı). Liste sonunda **Ders araçları** bölümü (4 tonlu-ikon giriş kartı, `_LessonToolTile`): **Dersler & Konular** (→`/study/catalog?studentId=`) · **Ödevlerim** (→`/student/assignments`) · **Öğretmenlerim** (→`/student/teacher`) · **Notlarım** (→`/study/notes?studentId=`). Widget'lar: `scheduling/presentation/scheduling_format.dart`, `scheduling/presentation/widgets/study_entry_form_sheet.dart`. |
| `/student/goals-overview` | `student_goals_overview_page.dart` | özet | **Hedefler** (Çalış sekmesi kısayollarından açılır, AppBar'lı): günlük/haftalık/net ilerleme çubukları (`getDashboard`) + "Hedefleri düzenle" (→goals) + Aylık/Üniversite "yakında" (backend alanı yok). |
| `/student/profile` | `student_profile_page.dart` | özet | **Profil sekmesi** (bottom nav'ın kök sekmesi, ux §11 — sayfa içi `AppPageHeader`, AppBar yok — Task 6, 2026-07-21): istatistik özeti (toplam çalışma/gün/rekor + toplam deneme/net + en çok çalışılan ders + rozet özeti, `getStreak`+`listSessions`+`listTests`+`getAchievements`) + **Ayarlar menüsü** (Velim · Gizlilik ayarları · Bildirim ayarları [yakında] · Abonelik [Faz 5, yakında] · Ayarlar & Güvenlik →`/account-info`) + **Çıkış yap** (bottom-sheet onayı, `AuthCubit.logout()`). Eski `/student/more` hub'ı bu sekmeye redirect eder (Task 6, sayfası silindi). |
| `/student/teacher` | `student_teacher_page.dart` | özet | **Öğretmenlerim** (Derslerim sekmesi → "Ders araçları" bölümünden açılır, AppBar'lı detay): **yalnızca bağlı öğretmen(ler)i** gösterir. Öğretmenler, güvenli öğrenci-kapsamlı derslerin (`GET /api/scheduling/students/{id}/lessons`, sahiplik `IStudentDirectory` ile doğrulanır) `teacherUserId` kümesinden türetilir; her biri için `GET /api/teachers/profiles/{userId}` ile profil çekilip **açılır-kapanır gradient hero kartı** gösterilir: varsayılan **yalnızca başlık** (navy→mavi degrade band: avatar + ad + doğrulama rozeti + başlık + branş pill'i + sağda aç/kapat oku); **oka/başlığa dokununca** gövde `AnimatedSize` ile açılır (deneyim/eğitim/ücret istatistik satırı + konum/format meta + "Hakkında" biyografi). Birden fazla öğretmen desteklenir (kart listesi + sayaç). Dersler burada listelenmez (Derslerim'de); Ödevler/Ders Notları/Mesajlar "yakında". |
| `/study/timer` | `study_timer_page.dart` (`StudyTimerCubit`) | aktif seans | **Başlangıç:** navy→teal gradient hero başlık + bölümlenmiş form: **Programından seç** (öğrencinin ders programındaki benzersiz dersler `SchedulingRepository.listStudentLessons` ile çekilir, hızlı seçim çipleri; çipe dokununca "Ders" alanı dolar) → `veya` ayracı → **Elle gir** (`AppTextField` ders/konu). → başlat. **Aktif seans:** dairesel kadran (net çalışma saati, HH:MM:SS) + durum rozeti; altında iki **canlı** metrik kartı: **Toplam süre** (net çalışma + mola) ve **Mola** (sayı + süre; yalnızca molada artar). Net çalışma süresi zaten merkezdeki kadranda gösterildiği için alt kartta tekrar edilmez. Sayaçlar cubit'te tek ticker ile izlenir: `elapsedSeconds` çalışırken, `breakSeconds` molada artar, `breakCount` her "Mola Ver" ile +1. **Bitir → çalışma özeti:** seans otomatik kapanmaz; başarı animasyonlu (elasticOut ✓) özet ekranı gösterilir. Özet metrikleri **sunucunun dakikaya yuvarlanmış değerine değil, kronometrede canlı izlenen `elapsedSeconds`/`breakSeconds` değerlerine** dayanır (kahraman metrik **net çalışma** + alt kartlar **mola** (sayı + süre) ve **toplam süre** = net+mola); `elapsedSeconds`/`breakSeconds`/`breakCount` "Kapat" (`acknowledgeSummary`) ile sıfırlanır. Kullanıcı **"Kapat"** ile kendi isteğiyle kapatır. Aktif seans: **dairesel kadran** — dıştaki halka geçen saniyeyi (0-60) gösterir, çalışırken teal nabız efekti; merkezde büyük saat (HH:MM:SS, tabular figürler) + durum rozeti (Çalışıyor/Molada). Altında **Net çalışma** ve **Mola** metrik kartları. Kontroller: Mola Ver/Devam Et (tonal) + Bitir (yeşil) + Seansı iptal et. Açılışta devam eden seansı geri yükler. `subject`/`topic` query param'ı ile ön-doldurulabilir ("Devam Et" akışı). |
| `/study/test` | `test_entry_page.dart` | form | Ders/konu/deneme adı/tür + toplam/doğru/yanlış/boş + süre; **canlı net önizleme**; `d+y+b=toplam` doğrulaması. |
| `/study/goals` | `study_goals_page.dart` | form | Günlük/haftalık/net hedef; veli & öğretmenle **paylaşım anahtarları** (çalışma/test ayrı). |
| `/study/history` | `study_history_page.dart` | sekmeler | Seanslar / Denemeler / Haftalık (gün-bazlı çubuk grafik + ders dağılımı) + **manuel seans** ekleme (bottom sheet). |
| `/study/achievements` | `achievements_page.dart` | liste | Rozet kataloğu + kazanım durumu + eşik ilerlemesi. |

## Navigasyon
- **Alt navigasyon (`StudentBottomNav`, 4 sekme — Task 1, 2026-08-19):** 🏠 Çalışma (`/student-home`) · 📚 Derslerim (`/student/lessons`) · 📊 Performans (`/student/performance`) · 👤 Profil (`/student/profile`). Keşfet sekmesi kaldırıldı (`student_discover_page.dart` silindi); `/student/discover` artık `/student/lessons`'a redirect'lenir. Eski rotalar redirect'lenir: `/student/tests`→`/student/performance`, `/student/calendar`→`/student/lessons`, `/student/studies`→`/student-home` (Task 3, 2026-07-21; sayfa silindi, içeriği Performans'a taşındı), `/student/more`→`/student/profile` (Task 6, 2026-07-21; sayfa silindi, "Diğer" hub'ı Profil'in Ayarlar menüsüne dağıtıldı). Sekme sayfaları `studentId`'yi bottom nav'dan almadığından `student_scope.dart` (`StudentScope.resolve`) ile oturumdan çözer (StudyHomeCubit ile aynı: M03 `by-user`, yoksa self-register).
- `student_home_page` hızlı işlemler bölümünden (2 sütunlu yatay kart `Wrap`'i) alt ekranlara `context.push('<route>?studentId=<id>')` ile gidilir (studentId query param). Kronometre başlangıç formu ayrıca `subject`/`topic` query param'ı ile ön-doldurulabilir.
- Ortak sekme bileşenleri: `presentation/widgets/study_tab_widgets.dart` (StudyCard · StudyStatTile · StudySectionHeader · StudySessionTile · StudyComingSoonCard).
- `app_router.dart`: `Student` (öğretmen değil) → `/student-home`; öğretmene özel ekranlara düşerse geri alınır.

## İlgili
- Teknik modül → [`../modules/m08_study.md`](../modules/m08_study.md) · Rol → [`../roles/ogrenci.md`](../roles/ogrenci.md)

---
*Öğrenci Bireysel Çalışma Ekranları | Güncelleme: 2026-07-21*
