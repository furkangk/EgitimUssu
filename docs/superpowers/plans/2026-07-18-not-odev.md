# Not & Ödev (Dilim B) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** M06'da not görünürlüğü (B-05) ve ödev onay/geri gönder + geri bildirim (T-06.7/8) eklemek.

**Architecture:** Assignments modülünün (`Assignment` + `LessonNote`) mevcut CQRS desenine additive. Yeni öğretmen komutları için ayrı `AssignmentTeacherFeatures.cs`. Her domain değişikliği için Assignments şemasına ayrı migration.

**Tech Stack:** .NET 9, EF Core (PostgreSQL, `assignments` şeması), xUnit (`tests/Unit`), CQRS + `Result`/`Error`, `IClock`.

## Global Constraints
- Modüller birbirine referans veremez; okuma `Shared/Contracts` üzerinden.
- Migration komutu: `dotnet ef migrations add <Ad> --project src/Modules/Assignments/Infrastructure --startup-project src/API.Host --context AssignmentsDbContext`
- Test: `dotnet test tests/Unit/EgitimUssu.Tests.Unit.csproj` · Build: `dotnet build EgitimUssu.sln`
- Zamanlar UTC + `IClock.UtcNow`.
- Commit sonu: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`
- Doküman bakımı son task'ta.

## File Structure
- `src/Modules/Assignments/Domain/AssignmentsDomainModel.cs` — `LessonNoteVisibility` enum + `LessonNote.Visibility`; `AssignmentStatus` yeni değerler; `Assignment.TeacherFeedback` + `Approve`/`ReturnForRevision`; yeni event'ler.
- `src/Modules/Assignments/Application/AssignmentFeatures.cs` — follow-up command/response'a visibility.
- `src/Modules/Assignments/Application/AssignmentTeacherFeatures.cs` *(yeni)* — approve/return command+handler+authorizer.
- `src/Modules/Assignments/API/AssignmentsModule.cs` — yeni endpoint'ler + DTO.
- `src/Modules/Assignments/Infrastructure/AssignmentsDbContext.cs` — config.
- `src/Modules/Assignments/Infrastructure/DependencyInjection.cs` — DI.
- `tests/Unit/LessonNoteTests.cs`, `tests/Unit/AssignmentTests.cs` *(yeni)*.

---

### Task 1: B-05 — Not görünürlüğü

**Files:**
- Modify: `src/Modules/Assignments/Domain/AssignmentsDomainModel.cs`
- Modify: `src/Modules/Assignments/Application/AssignmentFeatures.cs`
- Modify: `src/Modules/Assignments/API/AssignmentsModule.cs`
- Modify: `src/Modules/Assignments/Infrastructure/AssignmentsDbContext.cs`
- Test: `tests/Unit/LessonNoteTests.cs` (create)

**Interfaces:**
- Produces: `enum LessonNoteVisibility { Private = 1, Student = 2, StudentAndParent = 3 }`. `LessonNote` ctor sonuna `LessonNoteVisibility visibility`; `LessonNote.Visibility` get; `Update` imzasına `LessonNoteVisibility visibility`. `CreateLessonSessionFollowUpCommand`/`...Request` sonuna `LessonNoteVisibility Visibility = LessonNoteVisibility.Private`. `LessonNoteResponse` sonuna `string Visibility`.

- [ ] **Step 1: Write the failing test**

`tests/Unit/LessonNoteTests.cs`:
```csharp
using EgitimUssu.Modules.Assignments.Domain;

namespace EgitimUssu.Tests.Unit;

public sealed class LessonNoteTests
{
    private static readonly DateTime Now = new(2026, 7, 20, 12, 0, 0, DateTimeKind.Utc);

    [Fact]
    public void Ctor_DefaultsAndStoresVisibility()
    {
        var note = new LessonNote(
            Guid.NewGuid(), Guid.NewGuid(), Guid.NewGuid(), Guid.NewGuid(),
            "özet", "konu", "öneri", LessonNoteVisibility.StudentAndParent, Now);
        Assert.Equal(LessonNoteVisibility.StudentAndParent, note.Visibility);
    }

    [Fact]
    public void Update_ChangesVisibility()
    {
        var note = new LessonNote(
            Guid.NewGuid(), Guid.NewGuid(), Guid.NewGuid(), Guid.NewGuid(),
            "özet", null, null, LessonNoteVisibility.Private, Now);
        note.Update("özet2", null, null, LessonNoteVisibility.Student);
        Assert.Equal(LessonNoteVisibility.Student, note.Visibility);
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `dotnet test tests/Unit/EgitimUssu.Tests.Unit.csproj --filter FullyQualifiedName~LessonNoteTests`
Expected: FAIL — `LessonNoteVisibility` yok / ctor imzası uyuşmuyor.

- [ ] **Step 3: Update domain**

`AssignmentsDomainModel.cs`:
- Enum ekle (AssignmentStatus yanına):
```csharp
public enum LessonNoteVisibility
{
    Private = 1,
    Student = 2,
    StudentAndParent = 3
}
```
- `LessonNote` ctor imzasına `string? recommendations`'tan sonra `LessonNoteVisibility visibility` ekle; gövdeye `Visibility = visibility;`; property ekle `public LessonNoteVisibility Visibility { get; private set; }`.
- `LessonNote.Update` imzasına `string? recommendations`'tan sonra `LessonNoteVisibility visibility` ekle; gövdeye `Visibility = visibility;`.

- [ ] **Step 4: Run test to verify it passes**

Run: `dotnet test tests/Unit/EgitimUssu.Tests.Unit.csproj --filter FullyQualifiedName~LessonNoteTests`
Expected: PASS.

- [ ] **Step 5: Thread through Application + API + config**

`AssignmentFeatures.cs`:
- `CreateLessonSessionFollowUpCommand` sonuna `LessonNoteVisibility Visibility = LessonNoteVisibility.Private` ekle.
- `LessonNoteResponse` sonuna `string Visibility` ekle.
- Follow-up handler'daki `new LessonNote(...)` çağrısına `command.Visibility` ekle (recommendations sonrası). LessonNote→response eşlemesine `note.Visibility.ToString()` ekle (mapping metodu bul: `ToResponse` benzeri; yoksa response oluşturma satırında).

`AssignmentsModule.cs`:
- `CreateLessonSessionFollowUpRequest` sonuna `LessonNoteVisibility Visibility = LessonNoteVisibility.Private` ekle; `ToCommand`/dispatch içine geçir. (Dosya başına `using EgitimUssu.Modules.Assignments.Domain;` ekle — yoksa.)

`AssignmentsDbContext.cs` — `LessonNote` config'ine:
```csharp
        builder.Property(entity => entity.Visibility).HasConversion<string>().HasMaxLength(32).IsRequired();
```

- [ ] **Step 6: Migration + build + test**

Run: `dotnet ef migrations add AddLessonNoteVisibility --project src/Modules/Assignments/Infrastructure --startup-project src/API.Host --context AssignmentsDbContext`
Sonra `dotnet build EgitimUssu.sln` + `dotnet test tests/Unit/EgitimUssu.Tests.Unit.csproj`
Expected: `Visibility` kolonu; PASS.
> Migration `NOT NULL` kolonu ekliyor; mevcut satırlar için `defaultValue: "Private"` gerekebilir — üretilen migration'da `AddColumn<string>(..., nullable: false, defaultValue: "Private")` olacak şekilde düzenle.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat(assignments): ders notu görünürlük kontrolü (B-05)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: T-06.7/8 — Ödev onay / geri gönder + geri bildirim (domain)

**Files:**
- Modify: `src/Modules/Assignments/Domain/AssignmentsDomainModel.cs`
- Test: `tests/Unit/AssignmentTests.cs` (create)

**Interfaces:**
- Produces: `AssignmentStatus.Approved = 5`, `AssignmentStatus.ReturnedForRevision = 6`. `Assignment.TeacherFeedback` (string?). `Assignment.Approve(string? feedback, DateTime nowUtc)` → statü `Approved`. `Assignment.ReturnForRevision(string feedback, DateTime nowUtc)` → statü `ReturnedForRevision`. `SubmitWork`, `ReturnedForRevision` statüsünü de `InProgress`'e çeker. Event'ler: `AssignmentApprovedDomainEvent(Guid AssignmentId, Guid StudentId, Guid TeacherUserId, DateTime OnUtc)`, `AssignmentReturnedDomainEvent(...)`.

- [ ] **Step 1: Write the failing test**

`tests/Unit/AssignmentTests.cs`:
```csharp
using EgitimUssu.Modules.Assignments.Domain;

namespace EgitimUssu.Tests.Unit;

public sealed class AssignmentTests
{
    private static readonly DateTime Now = new(2026, 7, 20, 12, 0, 0, DateTimeKind.Utc);

    private static Assignment New(AssignmentStatus status = AssignmentStatus.Completed)
        => new(Guid.NewGuid(), Guid.NewGuid(), Guid.NewGuid(), null, "Ödev", null, null, status, null, Now, null);

    [Fact]
    public void Approve_SetsApprovedAndFeedback()
    {
        var a = New();
        a.Approve("eline sağlık", Now);
        Assert.Equal(AssignmentStatus.Approved, a.Status);
        Assert.Equal("eline sağlık", a.TeacherFeedback);
    }

    [Fact]
    public void ReturnForRevision_SetsReturnedAndFeedback()
    {
        var a = New();
        a.ReturnForRevision("2. soru eksik", Now);
        Assert.Equal(AssignmentStatus.ReturnedForRevision, a.Status);
        Assert.Equal("2. soru eksik", a.TeacherFeedback);
    }

    [Fact]
    public void SubmitWork_MovesReturnedToInProgress()
    {
        var a = New(AssignmentStatus.ReturnedForRevision);
        a.SubmitWork("https://file", Now);
        Assert.Equal(AssignmentStatus.InProgress, a.Status);
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `dotnet test tests/Unit/EgitimUssu.Tests.Unit.csproj --filter FullyQualifiedName~AssignmentTests`
Expected: FAIL — `Approve`/`ReturnForRevision`/yeni statüler yok.

- [ ] **Step 3: Update domain**

`AssignmentsDomainModel.cs`:
- `AssignmentStatus` enum'una ekle: `Approved = 5,` ve `ReturnedForRevision = 6`.
- `Assignment`'a property: `public string? TeacherFeedback { get; private set; }`.
- Metotlar:
```csharp
    public void Approve(string? feedback, DateTime nowUtc)
    {
        Status = AssignmentStatus.Approved;
        TeacherFeedback = feedback;
        Raise(new AssignmentApprovedDomainEvent(Id, StudentId, TeacherUserId, nowUtc));
    }

    public void ReturnForRevision(string feedback, DateTime nowUtc)
    {
        Status = AssignmentStatus.ReturnedForRevision;
        TeacherFeedback = feedback;
        Raise(new AssignmentReturnedDomainEvent(Id, StudentId, TeacherUserId, nowUtc));
    }
```
- `SubmitWork` içindeki `if (Status == AssignmentStatus.Pending)` koşulunu genişlet:
```csharp
        if (Status is AssignmentStatus.Pending or AssignmentStatus.ReturnedForRevision)
        {
            Status = AssignmentStatus.InProgress;
        }
```
- Event kayıtları (dosya sonundaki event'ler yanına):
```csharp
public sealed record AssignmentApprovedDomainEvent(
    Guid AssignmentId, Guid StudentId, Guid TeacherUserId, DateTime OnUtc) : DomainEvent;

public sealed record AssignmentReturnedDomainEvent(
    Guid AssignmentId, Guid StudentId, Guid TeacherUserId, DateTime OnUtc) : DomainEvent;
```

- [ ] **Step 4: Run test to verify it passes**

Run: `dotnet test tests/Unit/EgitimUssu.Tests.Unit.csproj --filter FullyQualifiedName~AssignmentTests`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat(assignments): ödev onay/geri gönder domain davranışı (T-06.7/8)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: T-06.7/8 — Approve/Return command + endpoint + geri bildirim alanı

**Files:**
- Create: `src/Modules/Assignments/Application/AssignmentTeacherFeatures.cs`
- Modify: `src/Modules/Assignments/Application/AssignmentFeatures.cs` (`AssignmentResponse` + mapping)
- Modify: `src/Modules/Assignments/API/AssignmentsModule.cs`
- Modify: `src/Modules/Assignments/Infrastructure/AssignmentsDbContext.cs`
- Modify: `src/Modules/Assignments/Infrastructure/DependencyInjection.cs`
- Test: `tests/Unit/AssignmentTeacherAuthorizerTests.cs` (create)

**Interfaces:**
- Produces: `ApproveAssignmentCommand(Guid AssignmentId, string? Feedback)`, `ReturnAssignmentCommand(Guid AssignmentId, string Feedback)`. Endpoint'ler `POST /api/assignments/{id}/approve`, `POST /api/assignments/{id}/return`. `AssignmentResponse` sonuna `string? TeacherFeedback`. Authorizer: assignment.TeacherUserId == currentUser (admin serbest).

- [ ] **Step 1: Write the failing test (authorizer reddi)**

`tests/Unit/AssignmentTeacherAuthorizerTests.cs` — mevcut `AssignmentStudentFeatures.cs` içindeki öğrenci authorizer desenini izle. `ICurrentUser` sahtesi + `IAssignmentRepository` sahtesi (yalnız `GetAssignmentByIdAsync`):
```csharp
using EgitimUssu.Modules.Assignments.Application;
using EgitimUssu.Modules.Assignments.Domain;
using EgitimUssu.Shared.Kernel;

namespace EgitimUssu.Tests.Unit;

public sealed class AssignmentTeacherAuthorizerTests
{
    private static readonly DateTime Now = new(2026, 7, 20, 12, 0, 0, DateTimeKind.Utc);

    private sealed class FakeCurrentUser : ICurrentUser
    {
        public string? UserId { get; init; }
        public bool IsAuthenticated => UserId is not null;
        public IReadOnlyCollection<string> Roles { get; init; } = new[] { "Teacher" };
        // Not: ICurrentUser'ın diğer üyeleri varsa, mevcut testlerdeki sahte uyarlamayı birebir izle.
    }

    private sealed class OneAssignmentRepo : IAssignmentRepository
    {
        private readonly Assignment _a;
        public OneAssignmentRepo(Assignment a) => _a = a;
        public Task<Assignment?> GetAssignmentByIdAsync(Guid id, CancellationToken ct) => Task.FromResult<Assignment?>(_a.Id == id ? _a : null);
        public Task<LessonNote?> GetLessonNoteByLessonSessionIdAsync(Guid id, CancellationToken ct) => throw new NotImplementedException();
        public Task<IReadOnlyCollection<Assignment>> ListByLessonSessionIdAsync(Guid id, CancellationToken ct) => throw new NotImplementedException();
        public Task<IReadOnlyCollection<Assignment>> ListAsync(Guid? t, Guid? s, Guid? l, CancellationToken ct) => throw new NotImplementedException();
        public Task AddLessonNoteAsync(LessonNote n, CancellationToken ct) => throw new NotImplementedException();
        public Task AddAssignmentsAsync(IEnumerable<Assignment> a, CancellationToken ct) => throw new NotImplementedException();
        public Task SaveChangesAsync(CancellationToken ct) => Task.CompletedTask;
    }

    [Fact]
    public async Task Approve_ByOtherTeacher_Forbidden()
    {
        var ownerTeacher = Guid.NewGuid();
        var assignment = new Assignment(Guid.NewGuid(), Guid.NewGuid(), ownerTeacher, null, "Ödev", null, null, AssignmentStatus.Completed, null, Now, null);
        var authorizer = new AssignmentTeacherAuthorizer(new FakeCurrentUser { UserId = Guid.NewGuid().ToString() }, new OneAssignmentRepo(assignment));

        var result = await authorizer.Authorize(new ApproveAssignmentCommand(assignment.Id, null), CancellationToken.None);
        Assert.False(result.IsSuccess);
    }
}
```
> Not: `ICurrentUser` arayüzünün tam üyelerini `AssignmentStudentFeatures.cs`/mevcut testlerden doğrula; `FakeCurrentUser`'ı ona göre tamamla.

- [ ] **Step 2: Run test to verify it fails**

Run: `dotnet test tests/Unit/EgitimUssu.Tests.Unit.csproj --filter FullyQualifiedName~AssignmentTeacherAuthorizerTests`
Expected: FAIL — `AssignmentTeacherAuthorizer`/`ApproveAssignmentCommand` yok.

- [ ] **Step 3: Create `AssignmentTeacherFeatures.cs`**

```csharp
using EgitimUssu.Modules.Assignments.Domain;
using EgitimUssu.Shared.Application;
using EgitimUssu.Shared.Kernel;

namespace EgitimUssu.Modules.Assignments.Application;

public sealed record ApproveAssignmentCommand(Guid AssignmentId, string? Feedback) : ICommand<Result<AssignmentResponse>>;

public sealed record ReturnAssignmentCommand(Guid AssignmentId, string Feedback) : ICommand<Result<AssignmentResponse>>;

public sealed class ApproveAssignmentCommandHandler : ICommandHandler<ApproveAssignmentCommand, Result<AssignmentResponse>>
{
    private static readonly Error NotFound = new("assignments.assignment_not_found", "Odev bulunamadi.");
    private readonly IAssignmentRepository _repository;
    private readonly IClock _clock;

    public ApproveAssignmentCommandHandler(IAssignmentRepository repository, IClock clock)
    {
        _repository = repository;
        _clock = clock;
    }

    public async Task<Result<AssignmentResponse>> Handle(ApproveAssignmentCommand command, CancellationToken cancellationToken)
    {
        var assignment = await _repository.GetAssignmentByIdAsync(command.AssignmentId, cancellationToken);
        if (assignment is null)
        {
            return Result<AssignmentResponse>.Failure(NotFound);
        }

        assignment.Approve(command.Feedback?.Trim(), _clock.UtcNow);
        await _repository.SaveChangesAsync(cancellationToken);
        return Result<AssignmentResponse>.Success(assignment.ToResponse());
    }
}

public sealed class ReturnAssignmentCommandHandler : ICommandHandler<ReturnAssignmentCommand, Result<AssignmentResponse>>
{
    private static readonly Error NotFound = new("assignments.assignment_not_found", "Odev bulunamadi.");
    private static readonly Error FeedbackRequired = new("assignments.feedback_required", "Geri gonderme icin aciklama zorunlu.");
    private readonly IAssignmentRepository _repository;
    private readonly IClock _clock;

    public ReturnAssignmentCommandHandler(IAssignmentRepository repository, IClock clock)
    {
        _repository = repository;
        _clock = clock;
    }

    public async Task<Result<AssignmentResponse>> Handle(ReturnAssignmentCommand command, CancellationToken cancellationToken)
    {
        if (string.IsNullOrWhiteSpace(command.Feedback))
        {
            return Result<AssignmentResponse>.Failure(FeedbackRequired);
        }

        var assignment = await _repository.GetAssignmentByIdAsync(command.AssignmentId, cancellationToken);
        if (assignment is null)
        {
            return Result<AssignmentResponse>.Failure(NotFound);
        }

        assignment.ReturnForRevision(command.Feedback.Trim(), _clock.UtcNow);
        await _repository.SaveChangesAsync(cancellationToken);
        return Result<AssignmentResponse>.Success(assignment.ToResponse());
    }
}

public sealed class AssignmentTeacherAuthorizer :
    ICommandAuthorizer<ApproveAssignmentCommand>,
    ICommandAuthorizer<ReturnAssignmentCommand>
{
    private static readonly Error Forbidden = new("shared.forbidden", "Bu odev uzerinde islem yapma yetkiniz yok.");
    private static readonly Error NotFound = new("assignments.assignment_not_found", "Odev bulunamadi.");
    private readonly ICurrentUser _currentUser;
    private readonly IAssignmentRepository _repository;

    public AssignmentTeacherAuthorizer(ICurrentUser currentUser, IAssignmentRepository repository)
    {
        _currentUser = currentUser;
        _repository = repository;
    }

    public Task<Result> Authorize(ApproveAssignmentCommand command, CancellationToken cancellationToken)
        => AuthorizeByAssignment(command.AssignmentId, cancellationToken);

    public Task<Result> Authorize(ReturnAssignmentCommand command, CancellationToken cancellationToken)
        => AuthorizeByAssignment(command.AssignmentId, cancellationToken);

    private async Task<Result> AuthorizeByAssignment(Guid assignmentId, CancellationToken cancellationToken)
    {
        var assignment = await _repository.GetAssignmentByIdAsync(assignmentId, cancellationToken);
        if (assignment is null)
        {
            return Result.Failure(NotFound);
        }

        if (_currentUser.Roles.Contains("Admin"))
        {
            return Result.Success();
        }

        return _currentUser.IsAuthenticated
            && Guid.TryParse(_currentUser.UserId, out var userId)
            && userId == assignment.TeacherUserId
            ? Result.Success()
            : Result.Failure(Forbidden);
    }
}
```
> `assignment.ToResponse()` mevcut mapping'i kullanır (AssignmentFeatures.cs'de). Erişilemiyorsa `internal` sınıf/metot görünürlüğünü doğrula (aynı Application namespace).

- [ ] **Step 4: Run test to verify it passes**

Run: `dotnet test tests/Unit/EgitimUssu.Tests.Unit.csproj --filter FullyQualifiedName~AssignmentTeacherAuthorizerTests`
Expected: PASS.

- [ ] **Step 5: Add TeacherFeedback to response + config**

`AssignmentFeatures.cs` — `AssignmentResponse` sonuna `string? TeacherFeedback` ekle; `ToResponse` eşlemesine `assignment.TeacherFeedback` ekle (uygun konum, `CompletedOnUtc`'den önce/sonra tutarlı).

`AssignmentsDbContext.cs` — `Assignment` config'ine:
```csharp
        builder.Property(entity => entity.TeacherFeedback).HasMaxLength(2000);
```

- [ ] **Step 6: Endpoints + DI**

`AssignmentsModule.cs` — endpoint'ler + handler:
```csharp
        group.MapPost("/{assignmentId:guid}/approve", ApproveAssignmentAsync)
        .WithSummary("Öğretmen ödevi onaylar");
        group.MapPost("/{assignmentId:guid}/return", ReturnAssignmentAsync)
        .WithSummary("Öğretmen ödevi geri bildirimle geri gönderir");
```
```csharp
    private static async Task<IResult> ApproveAssignmentAsync(
        HttpContext context, Guid assignmentId, ApproveAssignmentRequest request,
        ICommandDispatcher dispatcher, CancellationToken cancellationToken)
    {
        var result = await dispatcher.Dispatch(new ApproveAssignmentCommand(assignmentId, request.Feedback), cancellationToken);
        return ToHttpResult(context, result);
    }

    private static async Task<IResult> ReturnAssignmentAsync(
        HttpContext context, Guid assignmentId, ReturnAssignmentRequest request,
        ICommandDispatcher dispatcher, CancellationToken cancellationToken)
    {
        var result = await dispatcher.Dispatch(new ReturnAssignmentCommand(assignmentId, request.Feedback), cancellationToken);
        return ToHttpResult(context, result);
    }
```
DTO (dosyanın request bölümüne):
```csharp
public sealed record ApproveAssignmentRequest(string? Feedback);
public sealed record ReturnAssignmentRequest(string Feedback);
```
`ToHttpResult` (Assignments) switch'inde `assignments.assignment_not_found` → 404 ve `assignments.feedback_required` → 400 varsa uygun; yoksa varsayılan 400 yeterli. `assignments.assignment_not_found` için 404 ekle.

`DependencyInjection.cs`:
```csharp
        services.AddScoped<ICommandHandler<ApproveAssignmentCommand, Result<AssignmentResponse>>, ApproveAssignmentCommandHandler>();
        services.AddScoped<ICommandHandler<ReturnAssignmentCommand, Result<AssignmentResponse>>, ReturnAssignmentCommandHandler>();
        services.AddScoped<ICommandAuthorizer<ApproveAssignmentCommand>, AssignmentTeacherAuthorizer>();
        services.AddScoped<ICommandAuthorizer<ReturnAssignmentCommand>, AssignmentTeacherAuthorizer>();
```

- [ ] **Step 7: Migration + build + test**

Run: `dotnet ef migrations add AddAssignmentTeacherFeedback --project src/Modules/Assignments/Infrastructure --startup-project src/API.Host --context AssignmentsDbContext`
Sonra `dotnet build EgitimUssu.sln` + `dotnet test tests/Unit/EgitimUssu.Tests.Unit.csproj`
Expected: `TeacherFeedback` kolonu; PASS.

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "feat(assignments): ödev onay/geri gönder endpoint + geri bildirim (T-06.7/8)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: Dokümantasyon

**Files:** `doc/modules/m06_assignments.md`, `doc/modules/00_genel_bakis.md`, `doc/modules/veri_modeli.md`, `doc/roles/ogretmen.md`

- [ ] **Step 1: m06_assignments.md** — `LessonNoteVisibility`, `Assignment` yeni statüler + `TeacherFeedback`, yeni endpoint'ler (`/approve`, `/return`), ödev durum makinesi güncel. Tarih 2026-07-18.
- [ ] **Step 2: 00_genel_bakis.md** — Assignments endpoint envanterine `/approve` + `/return` ekle.
- [ ] **Step 3: veri_modeli.md** — `LessonNote.Visibility`, `Assignment.TeacherFeedback` + yeni statüler.
- [ ] **Step 4: ogretmen.md §10** — B-05 + T-06.7/8 "✅ yapıldı (Dilim B)"; ilgili Kabul Kriteri `[x]`.
- [ ] **Step 5: Commit**
```bash
git add -A
git commit -m "docs: not & ödev (Dilim B) doküman güncellemesi

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review Notu
- **Spec kapsamı:** B-05 (Task 1), T-06.7/8 (Task 2 domain + Task 3 endpoint). Karşılandı.
- **Tip tutarlılığı:** `LessonNoteVisibility`, `AssignmentStatus.Approved/ReturnedForRevision`, `TeacherFeedback` adları task'lar arası tutarlı. `ToResponse` mevcut mapping yeniden kullanılır.
- **Doğrulama gereği:** `ICurrentUser` üyeleri Task 3 Step 1'de mevcut testlerden doğrulanır; `AssignmentResponse.ToResponse` görünürlüğü aynı namespace'te.
